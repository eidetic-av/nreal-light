using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NRKernal;
using Eidetic.URack.Collection;

public class TestController : MonoBehaviour
{
    public Mirage2x Mirage2x;

    void Update()
    {
        // if (Mirage2x == null)
        //     Mirage2x = GameObject.Find("Mirage2xInstance280").GetComponent<Mirage2x>();
        // var handPos = NRInput.GetPosition();
        // Mirage2x.PositionX = handPos.x;
        // Mirage2x.PositionY = handPos.y;
        // Mirage2x.PositionZ = handPos.z;
    }
}
